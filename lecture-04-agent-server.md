# 엔디의 코딩에이전트 만들기 #4 — 에이전트 서버 제작

기존 3개 강의 슬라이드의 분량·구조를 그대로 따르는 4강 원고다. 각 슬라이드는 **제목 / 핵심 한 줄 / 번호 항목 / 대시 하위불릿** 구조이며, 슬라이드 본문 아래의 `참조`·`노트`는 발표자용이므로 슬라이드에는 넣지 않는다.

## 기존 강의 분석 (원고 작성 기준)

| 강의 | 총 장수 | 섹션 | 구성 |
| --- | --- | --- | --- |
| #1 에이전틱AI의 이해 | 12 | 2 (에이전트란 무엇인가 / 도구와 훅) | 표지1 + 섹션표지2 + 본문9 |
| #2 비지니스용 에이전트 | 10 | 3 (세션과 컨텍스트 / 비지니스용 에이전트 / 실제 개발 계획) | 표지1 + 섹션표지3 + 본문6 |
| #3 관리자 제작 | 10 | 3 (현재 #2와 동일 본문이 들어가 있음 — 재작성 필요) | 표지1 + 섹션표지3 + 본문6 |

공통 규칙: 본문 슬라이드는 `핵심:` 한 줄 + 번호 항목 3~5개, 각 항목에 대시 하위불릿 2~6개. 개념 → 선택지 비교 → 구현 판단 순으로 서술하고, 구현 여부는 `→구현 / →미구현 / →간략하게 구현`으로 표기한다.

**#4 분량: 14장 (표지1 + 섹션표지3 + 본문10)** — #2/#3과 동일한 3섹션 구조 + 마지막에 부하 개념 정리 슬라이드 1장 추가.

---

# 표지 (1장)

```
ND
엔디의
코딩에이전트 만들기
#4 에이전트 서버 제작
```

---

# 섹션 1. 구축 인프라와 이벤트 프로토콜

## 슬라이드 2 — 섹션 표지

```
구축 인프라와 이벤트 프로토콜
```

## 슬라이드 3 — 구축 인프라: 왜 일반 테이블이 아니라 PGMQ(큐)를 쓰는가

핵심: 할 일 목록을 테이블에 적어두는 것과, '지금 이거 내가 처리 중이니 남은 못 가져가'라고 표시하며 나눠주는 것은 다른 문제다

1. 테이블에 그냥 쌓아두면 생기는 문제
   - 여러 일꾼(워커)이 동시에 할 일 목록을 보면, 같은 일을 서로 집어가려고 부딪힘
   - 한 일꾼이 일을 집은 채로 갑자기 죽으면, 그 일은 "처리 중"으로 표시된 채 영원히 멈춰버림 — 아무도 다시 안 가져감
   - 새 일이 들어왔는지 계속 눈으로 확인(폴링)해야 하는데, 자주 보면 컴퓨터가 헛일을 많이 하고, 뜸하게 보면 처리가 늦어짐
2. 그럼 카카오톡/문자 같은 전문 메세지 시스템(Kafka 등)을 쓰면?
   - 그건 그것대로 별도의 프로그램·별도의 서버를 하나 더 설치하고 돌봐야 함(백업, 장애 대응 다 따로)
   - 우리 데이터는 PostgreSQL에 있는데 메세지만 다른 시스템에 있으면 "메세지는 보냈는데 DB 저장은 실패" 같은 어긋남이 생길 수 있음
   - 우리가 지금 쓰는 방식(한 번 받은 일은 절대 두 번 처리 안 하기)과 잘 안 맞는 도구도 있음(Kafka)
3. PGMQ는 이 문제를 이미 풀어놓은 부품
   - 우리가 쓰는 PostgreSQL 안에 "줄 서기 기능"만 추가로 설치하는 것(설치 명령 한 줄)
   - "일 하나 꺼내가기 / 다 됐으니 지우기 / 아직 하는 중이니 시간 더 달라고 하기" — 이 3가지만 잘 되면 충분
   - 여러 일꾼이 동시에 꺼내가도 서로 안 부딪히게 알아서 처리해줌
4. 속도는 충분한가
   - 이 줄서기 기능 자체는 1초에 수천 건도 거뜬히 처리함
   - 반면 우리 서비스는 사람 1명이 1초에 몇 건 요청하는 수준 — 즉 줄서기 기능은 실제 필요량보다 훨씬 여유가 있음
   - 실제로 느려지는 구간은 큐가 아니라 AI 모델이 답변을 생각하는 시간(몇 초~몇십 초) — 큐를 더 빠르게 만들어도 체감 속도는 그대로

참조: `apps/admin/docker/postgres/initdb/001-extensions.sql`, `apps/agent/src/server/pgmq/client.ts`, `apps/agent/docs/constraints.md`, `docker-compose.yml`
노트: 어려운 용어(가시성 타임아웃, SKIP LOCKED 등)는 구두로 곁들이되 슬라이드 문구에는 넣지 않는다. 핵심은 "테이블은 안전한 줄서기를 못 해주고, 전문 메세지 시스템은 과하다"는 두 문장으로 요약.

---

## 슬라이드 4 — 턴루프를 이벤트로 분해하는 이유

핵심: 인메모리 턴루프는 한 프로세스의 수명에 묶이지만, 이벤트로 분해하면 수명·장애·클라이언트에서 분리된다

1. 인메모리 루프의 한계
   - 1강의 턴루프(요청→이터레이션→도구호출→최종응답)를 함수 호출로 구현하면 프로세스가 죽는 순간 진행 중인 턴이 사라짐
   - 도구 실행이 길어지면 루프가 블록되어 인터럽트·스티어링을 받을 수 없음
   - 브라우저·CLI·다른 에이전트가 같은 세션에 동시에 붙을 수 없음
2. 이벤트 소싱으로의 전환
   - 2강 결론: 세션은 세션ID로만 식별되는 끝없는 메세지 스트림
   - 루프의 각 단계 전이를 이벤트로 기록하면 컨텍스트는 이벤트 로그의 투영(projection)이 됨
   - 재시작·재생·감사(audit)·컴팩트가 전부 같은 로그 위에서 처리됨
3. 이벤트 기반이 만드는 제약
   - 인메모리 참조를 넘길 수 없으므로 모든 경계에서 엄격한 직렬화 필요(1강 '프로세스 도구 실행'과 동일한 대가)
   - 큐는 기본적으로 at-least-once이므로 멱등성 설계가 선택이 아니라 전제
   - 이벤트가 곧 API이므로 타입 정의가 서버·워커·클라이언트의 유일한 계약
4. 이번 강의의 범위
   - 프로토콜 타입 → 큐 전송 → 컨슈머 → 워커 → 스트림 → 테스트 클라이언트까지의 골격 →구현
   - 모델 호출·컨텍스트 저장·컴팩트 실행 본체 →미구현(다음 강의)

참조: `packages/agent_domain/src/common/protocol/envelope/index.ts`, `apps/agent/docs/overview.md`
노트: 1강의 '비동기와 상태대기', 2강의 '세션처리 방법'을 그대로 이어받는 슬라이드라고 못박고 시작한다.

## 슬라이드 5 — 이벤트 봉투 설계

핵심: 모든 이벤트가 공유하는 봉투(envelope) 하나가 라우팅·재시도·추적을 전부 감당한다

1. 식별 키
   - `eventKey`: 이벤트 1건의 고유 ID(중복 저장 방지용 PK)
   - `transactionKey`: 재시도해도 같은 값을 유지하는 멱등성 경계
   - `parentEventKey` / `causationEventKey` / `correlationKey`: 인과 그래프 복원용
2. 계층 컨텍스트
   - `sessionKey` → `runKey` → `turnKey` → `iterationKey` → `stepKey` → `toolCallKey`
   - 1강의 세션/턴/이터레이션 3계층을 그대로 필드로 승격
   - 어느 이벤트든 자기 위치를 스스로 설명하므로 서버는 상태를 몰라도 라우팅 가능
3. 분류 축
   - `kind`: request / response / progress / notice / heartbeat — 흐름상의 역할
   - `state`: queued / accepted / running / waiting / completed / failed / cancelled / timed_out — 실행 상태
   - `scope`: session / run / turn / iteration / step / tool / process / event — 영향 범위
   - `sequence`: 스트림 재정렬 및 누락 감지용 단조 증가 번호
4. 채널과 결과 계약
   - `channel` / `replyChannel`: 요청과 결과를 서로 다른 채널로 분리(CPS 스타일)
   - `AgentResult<T>` = `{ok:true, value}` | `{ok:false, error}` — 성공/실패를 타입으로 강제
   - `AgentError.retryable`: 재시도 가능 여부를 발생 지점이 판정해서 실어보냄

참조: `packages/agent_domain/src/common/protocol/envelope/index.ts`
노트: "이 한 장이 전체 서버의 계약이다. 나머지는 payload만 다르다"로 정리.

## 슬라이드 6 — 멱등성: 우리가 실제로 하는 건 '중복 차단'이지 '멱등 구현'이 아니다

핵심: 원격 통신은 원리적으로 "정확히 한 번 전달"을 보장할 수 없다 — 중복은 버그가 아니라 네트워크의 성질이다. 그래서 우리가 만든 건 모든 동작을 멱등하게 만든 게 아니라, 키 하나로 재실행을 막는 검문소다

1. 중복은 언젠가 반드시 일어난다(네트워크의 본질)
   - 요청을 보내고 응답을 못 받으면, "실행은 됐는데 응답만 유실"과 "실행 자체가 안 됨"을 보낸 쪽은 구분할 수 없음
   - 구분할 방법이 없으니 안전한 선택은 하나뿐: 다시 보낸다 — 이게 모든 재시도 로직의 근거
   - 즉 "정확히 한 번 전달(exactly-once delivery)"은 설계를 잘해서 얻는 게 아니라, 애초에 존재하지 않는 목표다. 우리가 실제로 살 수 있는 건 "최소 한 번은 전달"뿐
2. 그래서 우리가 만든 건 '진짜 멱등'이 아니다
   - 진짜 멱등이란 그 동작 자체를 몇 번 실행해도 결과가 저절로 같아지는 것(예: "잔액을 0으로 만들어라"는 몇 번 해도 결과가 같음)
   - 우리의 도구 호출·파일 수정·메세지 발송은 대부분 그런 성질이 없음 — 매 동작마다 멱등하게 다시 설계하는 건 동작 수만큼 중복 구현이 필요해서 현실적으로 불가능
   - 그래서 택한 방법: 모든 동작 앞에 '이 키를 이미 처리했는가'만 확인하는 검문소 하나를 세우고, 동작 자체는 그대로 둔 채 통과 여부만 키로 결정
   - 이건 엄밀히 말하면 동작이 멱등해진 게 아니라, 실행 여부가 키라는 인자에 의해 결정되는 것 — 인자 기반 분기이지 멱등 동작이 아니다. 다만 이 검문소 하나로 모든 동작의 개별 멱등 구현을 대체할 수 있어 비용을 감당할 수 있는 크기로 줄인 것
3. 이벤트 기반 구조는 이 상황을 더 자주 만든다
   - 요청·응답이 동기 호출 한 쌍이 아니라 대량의 비동기 이벤트로 흩어져 오가고, 그 사이의 도착 순서를 보장하지 않음
   - 순서가 보장되지 않으므로 "먼저 보낸 게 먼저 처리된다"는 가정 자체를 코드 어디에서도 할 수 없음 — 재정렬·중복·역전이 항상 같이 옴
4. CPS를 지향할수록 사태는 더 커진다
   - CPS(Continuation-Passing Style) 짧은 소개: 보통 호출은 "요청하고 그 자리에서 응답을 기다림"이지만, CPS는 "요청과 함께 '응답을 나중에 받을 곳(continuation)'만 던져두고 부른 쪽은 바로 다음 일로 넘어감" — 응답은 훗날 그 목적지로 알아서 배달됨
   - 우리 프로토콜에서 그 목적지가 `replyChannel`(슬라이드 5) — 요청 이벤트에 결과를 받을 채널 이름을 실어 보내고, 결과는 완전히 별도의 이벤트로 그 채널에 도착
   - 요청-응답의 물리적 연결이 끊어져 있으므로, 어느 한쪽만 보고는 상대가 처리했는지 알 방법이 원천적으로 없음
   - 이 구조를 택한 순간 중복·재정렬 대응은 선택 사항이 아니라 이 아키텍처의 존재 조건이 됨
5. 그래서 검문소의 기준을 '키' 하나로 강제로 좁힌다
   - 모든 요청에 세상에 하나뿐인 이름표(`transactionKey`)를 붙이고, 검문소는 오직 이 키만 봄
   - 같은 키가 다시 오면 동작을 다시 실행하지 않고 저장된 결과를 그대로 돌려줌 — 실제 구현은 뒤(멱등성과 재생) 슬라이드에서 이어감

참조: `packages/agent_domain/src/common/protocol/envelope/index.ts`(`transactionKey`, `channel`/`replyChannel`)
노트: "우리가 만든 건 멱등이 아니라 키 검문소다"라는 문장을 반드시 짚고 넘어간다. 이걸 안 짚으면 학생들이 "재시도해도 결과가 같으니 멱등"이라고 오해한 채로 다음 슬라이드(멱등성과 재생)의 구현을 본다.

## 슬라이드 7 — 이벤트 패밀리 10종

핵심: 턴루프의 각 관심사를 파일 단위 패밀리로 나누고, 유니온 하나로 다시 합친다

1. 세션·턴·이터레이션
   - `session.*`: create / get / delete / cancel / resume + `SessionStatus`(active, compacting, waiting …)
   - `turn.*`: start / input.append / stop / final / cancelled / failed + `usage` 토큰 집계
   - `iteration.*`: start / progress(델타·리즈닝) / merge / merged / failed + `nextAction: continue | complete`
2. 도구·프로세스
   - `tool.*`: call / started / progress / stdout / stderr / completed / failed / cancel — 1강 '도구의 출력' 부수효과를 전부 이벤트화
   - `process.*`: 외부 프로세스 실행의 stdout/stderr/exit/timeout/cancel
   - 도구는 논리적 호출, 프로세스는 물리적 실행으로 분리해 어떤 언어의 프로그램도 붙일 수 있게 함
3. 훅·모델·상태
   - `hook.*`: before/after × session·turn·iteration·tool·compaction 10개 지점 + `mutations`로 컨텍스트 개입
   - `model.*`: 모델 선택·변경·기본값 갱신 → 2강의 모델라우터가 붙는 자리
   - `state.*`: kv put/get/delete/persist, `compaction.*`, `checkpoint.*` — 2강 컴팩트 전략의 실행 인터페이스
4. 승인·산출물
   - `approval.*`: request / granted / rejected / expired + `expiresAt` — 가드레일이 턴을 멈추는 지점
   - `artifact.*`: file / diff / patch / log / screenshot 등록과 `contentHash`
5. 합성 — 나눈 걸 왜 다시 하나로 묶는가
   - 10개 패밀리는 파일도 타입도 서로 다른 별개 정의임. 그런데 이벤트가 실제로 지나가는 통로(큐 전송, 이벤트 로그, WebSocket 허브)는 "이게 세션 이벤트인지 도구 이벤트인지" 몰라도 되고, 그냥 받아서 넘겨주기만 하면 됨
   - 합치지 않으면 그 통로 함수마다 "session이면 이렇게, tool이면 저렇게…" 10가지 타입을 각각 받는 함수를 10벌 만들거나, 타입 검사를 포기하고 아무거나 받는 `any`로 때워야 함
   - `VibeEvent`는 이 10개 패밀리를 "A 또는 B 또는 C …" 식으로 묶은 합집합(union) 타입 — 큐·로그·허브는 `VibeEvent` 하나만 받는 함수 한 벌로 끝나면서도, 그 안에 실제로 어떤 패밀리가 들어있는지는 정보로 그대로 남아 있음
   - 비유: 우편함(큐)은 편지 종류를 몰라도 받고 배달만 하면 되고, 받는 사람이 봉투에 적힌 문구(action)를 보고서야 무슨 편지인지 판단하는 것과 같음
   - `action` 값(`tool.call.request` 같은 문자열)을 확인하는 순간 TypeScript가 "이건 ToolEvent다"라고 자동으로 좁혀줌(discriminated union) — 그 뒤 코드에서는 그 이벤트를 안전하게 ToolEvent로 다룰 수 있음
   - 새 기능을 추가할 때는 새 패밀리 파일 하나 + 이 유니온에 한 줄만 추가하면 끝, 큐·로그·허브 쪽 코드는 전혀 건드리지 않음 →확장 비용이 상수

참조: `packages/agent_domain/src/common/protocol/{session,turn,iteration,tool,hook,model,state,process,approval,artifact}/index.ts`, `.../protocol/vibe/index.ts`
노트: 파일 하나당 6~9줄로 유지되는 것을 보여주고, "타입이 곧 문서"임을 강조.

---

# 섹션 2. 큐와 실행

## 슬라이드 8 — 섹션 표지

```
큐와 실행
```

## 슬라이드 9 — 큐를 어떻게 처리하는가: 접수창구와 배달회사를 나눈다

핵심: 우리 코드는 "꺼내기 / 지우기 / 시간 더 달라기" 세 동작만 알면 되고, 실제로 그 일을 해주는 회사(PGMQ)는 언제든 갈아끼울 수 있어야 한다

1. 창구 직원과 배달회사를 분리한다
   - 우리 서버(컨슈머)는 "일 하나 꺼내오기 / 다 했으니 지우기 / 아직 하는 중이니 시간 더 달라고 하기" 세 가지 동작만 앎
   - 이 세 동작을 실제로 처리하는 게 지금은 PGMQ지만, 나중에 다른 회사(SQS 등)로 바꿔도 창구 직원(우리 코드)은 그대로 일함
   - 비유: 택배 접수처 직원이 "택배 보내기/조회/취소"만 알면 되고, 실제로 어느 택배회사가 배달하는지는 몰라도 되는 것과 같음
2. 실제로 도는 방식: 하나 꺼내서 처리하고, 다시 하나 꺼낸다
   - 할 일이 없으면 잠깐 기다렸다가 다시 확인 — 도착하면 바로 알아채고, 없으면 컴퓨터를 헛돌리지 않음(슬라이드 3의 "줄서기 기능")
   - 처리 도중 에러가 나도 창구 자체는 안 죽고, 잠깐 쉬었다가 계속 다음 일을 받음
3. 오래 걸리는 일은 "아직 하는 중"이라고 계속 알려야 한다
   - 비유: 화장실 칸의 "사용 중" 표시를 계속 갱신하지 않으면 남이 문을 열고 들어옴
   - 처리 시간이 길어지는 작업은 중간중간 "나 아직 하고 있어, 남이 못 가져가게 시간 좀 더 줘"라고 스스로 알려야 함
   - 이걸 안 하면 오래 걸리는 작업 하나가 두 명에게 동시에 배정되는 사고가 남
4. 언제 지우는가
   - 결과를 다 보내고, 저장까지 끝난 뒤에만 원본을 지움 — 순서를 앞당기면 중간에 죽었을 때 "일은 했는데 기록이 통째로 사라지는" 사고가 남
   - 먼저 지우지 않고 늦게 지우기 때문에 같은 일이 중복 처리될 수도 있는데, 그건 앞 슬라이드의 멱등성 검문소가 걸러줌 — 그래서 "결과 확정 후 삭제"가 가장 안전한 순서

참조: `apps/agent/src/server/queue/transport.ts`, `apps/agent/src/server/pgmq/client.ts`, `apps/agent/src/server/consumer.ts`, `apps/agent/docs/constraints.md`
노트: 삭제 시점 규약은 화이트보드로 타임라인을 그려 각 시점에 서버가 죽으면 무슨 일이 생기는지 확인.

## 슬라이드 10 — 멱등성을 실제로 어떻게 구현했나

핵심: "이 이름표(transactionKey) 처리한 적 있나?"를 표 하나로 확인하는 검문소를 DB에 세운다

1. 검문소는 표(테이블) 하나다
   - 이름표를 주민번호처럼 유일한 값으로 쓰는 표를 하나 만들어둠
   - 새 요청이 오면 그 표에 "먼저 등록하는 사람이 임자" 방식으로 끼워넣기를 시도함
   - 끼워넣기에 성공하면 내가 이 요청의 처리 담당, 실패하면(이미 있으면) 누가 먼저 다녀간 것
2. 이미 있는 경우 세 갈래로 나뉜다
   - 그 사람이 이미 다 끝냈으면 → 다시 실행하지 않고 예전 결과를 그대로 돌려줌
   - 아직 처리 중이면 → 끝날 때까지 기다림
   - 같은 이름표인데 요청 내용이 다르면 → 이름표를 실수로 재사용한 것이므로 조용히 넘어가지 않고 바로 오류로 알림(내용이 같은지는 요청 내용의 지문을 찍어서 비교)
3. 결과를 통째로 보관해두는 이유
   - 재실행하지 않고 그대로 돌려주려면 "예전에 무엇을 돌려줬는지"를 어딘가 보관해야 함 — 그 보관함이 바로 이 표
   - 성공/실패/시간초과/취소를 구분해서 저장해두면, 나중에 각각 다른 재시도 규칙을 적용할 수 있음
4. 오간 이벤트를 전부 따로 기록해두는 이유
   - 처리 여부만 표에 남기는 것과 별개로, 오간 모든 이벤트를 시간순으로 전부 저장해둠
   - 문제가 생겼을 때 "이 요청이 정확히 어떤 경로로 처리됐는지"를 나중에 그대로 재구성할 수 있음 — 기록이 없으면 원인 추적이 불가능함

참조: `apps/agent/src/server/execution/store.ts`, `apps/agent/src/server/event-log.ts`
노트: 같은 요청을 2회 보내서 두 번째 응답이 실제 작업 없이 즉시 예전 결과로 돌아오는 것을 시연.

## 슬라이드 11 — 실제 작업은 다른 일꾼에게 맡긴다

핵심: 접수창구와 실제 작업자를 분리해두면, 무거운 작업 하나가 창구 전체를 멈추게 하지 않는다

1. 왜 본체가 직접 일하지 않는가
   - 무거운 작업 하나가 오래 걸리면, 접수창구까지 같이 멈춰서 다른 요청을 아예 못 받게 됨
   - 그래서 "요청을 받는 일"과 "실제로 처리하는 일"을 서로 다른 일꾼에게 맡겨 분리함
   - 일꾼이 갑자기 사고(오류로 죽음)를 당해도 창구는 안 죽고, 새 일꾼을 바로 채용해서 자리를 메움
2. 일꾼은 필요할 때만 뽑는다
   - 할 일이 없으면 일꾼 수는 0명 — 아무도 놀고 있지 않음
   - 일이 밀리기 시작하면 정해둔 최대 인원까지만 늘림 — 사람을 무한정 고용하지 않고 자원을 한도 안에서 씀
3. 중간에 그만두게 하기와 시간 제한
   - "이제 그만해"라는 신호를 보내면 일꾼이 하던 작업을 중간에 멈춤(끝까지 강제로 시킬 수 없는 상황에서 필요)
   - 정해진 시간 안에 안 끝나면 자동으로 중단시킴 — 한 작업이 무한정 자리를 차지하지 않게 함
   - 다만 이게 가능하려면 작업이 잘게 쪼개져 있어서 중간에 멈출 지점이 있어야 함
4. 아무 일이나 시키지 않는다
   - 미리 허락해둔 작업 목록에 있는 것만 시킴
   - 목록에 없는 낯선 작업이 오면 시도조차 하지 않고 바로 실패로 처리 — 검증 안 된 코드를 무작정 실행하는 위험을 막음
   - 지금은 데모 작업 몇 개만 연결돼 있고, 실제 AI 호출·셸 명령 실행은 다음 단계 →미구현

참조: `apps/agent/src/server/worker/pool.ts`, `apps/agent/src/server/worker/entry.ts`, `apps/agent/src/server/env.ts`
노트: 일꾼 수가 0에서 시작해 필요할 때만 늘어나는 로그를 실제로 띄워 보여준다.

---

# 섹션 3. 스트림과 클라이언트

## 슬라이드 12 — 섹션 표지

```
스트림과 클라이언트
```

## 슬라이드 13 — 스트림 허브와 이중 인그레스

핵심: 요청은 HTTP·WebSocket 어디로 들어와도 같은 이벤트가 되고, 구독자는 채널로만 걸러 받는다

1. 들어오는 문은 두 개, 그 뒤는 하나
   - 요청은 "한 번 보내고 끝"(HTTP)으로 오거나 "연결해두고 계속 주고받기"(WebSocket)로 옴
   - 어느 문으로 들어오든 문을 통과하는 순간 완전히 같은 이벤트로 바뀜 — 뒤쪽 로직은 어느 문으로 왔는지 신경 쓰지 않음
   - 접수하자마자 "잘 받았다"는 신호를 곧바로 스트림에 흘려보내서, 보낸 사람이 자기 요청이 접수됐는지 바로 확인 가능
2. 실시간 연결은 방송 채널 구독과 같다
   - 원하는 채널만 골라서 듣고, 언제든 채널을 바꿔 들을 수 있음(비유: 라디오 주파수 맞추듯)
   - 방송국(허브)은 "누가 어느 채널을 듣고 있는지"만 보고, 그 채널에 해당하는 소식만 그 사람에게 흘려보냄
   - 연결을 하나씩 따로 관리하지 않고 방송하듯 뿌리기만 하므로, 듣는 사람이 늘어도 방송국 쪽 부담이 크게 늘지 않음
3. 연결이 끊겨도 정보는 안 사라진다
   - 방송을 듣던 중 연결이 끊겨도, 처리 기록 자체는 큐와 DB에 그대로 남아 있음
   - 다시 접속하면 이어서 새 소식을 받을 수 있음 — 다만 "끊긴 동안 놓친 걸 되감아 다시 보여주는" 기능은 아직 없음
4. 서버가 살아있는지와 일할 준비가 됐는지는 다르다
   - "프로세스가 죽지 않고 떠 있는가"와 "DB·큐에 연결돼서 실제로 일을 받을 수 있는가"를 서로 다른 확인 창구로 나눔
   - 프로세스는 떠 있어도 DB가 아직 안 붙었으면 "아직 준비 안 됨"으로 정직하게 답함 — 컨테이너가 이걸 보고 트래픽을 보낼지 판단
   - 남은 과제: 끊긴 동안 놓친 소식 재생, 채널별로 누가 볼 수 있는지 권한 제한 →미구현

참조: `apps/agent/src/server/app.ts`, `apps/agent/src/server/stream/hub.ts`, `apps/agent/src/server/transport/websocket.ts`, `apps/agent/src/server/index.ts`

## 슬라이드 14 — 눈으로 확인하는 테스트 콘솔

핵심: 프로토콜이 맞게 동작하는지는 코드 리뷰가 아니라 사람이 직접 눌러보는 화면으로 증명한다

1. 화면 구성
   - 왼쪽엔 채널 목록(원하는 채널만 켜고 끌 수 있음), 가운데엔 오간 이벤트가 시간순으로 쌓이는 타임라인, 오른쪽엔 이벤트를 직접 만들어 보내는 입력창
   - 입력창의 종류 목록은 실제 프로토콜에 정의된 이벤트 종류를 그대로 보여줌 — 문서와 화면이 따로 놀지 않음
   - 각 이벤트 옆에 상태(대기중/처리중/도착/실패)와 이름표(transactionKey)를 같이 보여줘서, 재생과 중복을 눈으로 바로 구분 가능
2. 두 가지 확인 모드
   - 서버 없이: 흐름만 화면에서 순서대로 재생해서 보여줌 — 프로토콜 설계 자체가 말이 되는지만 먼저 확인
   - 서버와 실제 연결: 진짜로 보내고 실제 응답이 돌아오는 왕복까지 확인
   - 연결이 끊겨 있으면 실패했다는 걸 화면에 스스로 표시 — 오프라인 상태에서도 흐름 학습은 가능
3. 화면 코드와 서버 코드는 같은 정의를 공유한다
   - 이벤트 목록·상태 값 등은 화면 쪽에서 새로 만들지 않고, 서버와 같은 곳에 정의된 것을 그대로 가져다 씀
   - 정의가 하나뿐이므로 서버 쪽 이벤트 종류가 늘어나도 화면 목록이 저절로 따라감
4. 실제로 확인하는 절차
   - 서버를 새로 띄운 뒤 "일할 준비가 됐는지" 먼저 확인
   - 콘솔에서 같은 이름표로 요청을 두 번 보내서, 두 번째가 재작업 없이 예전 결과를 그대로 돌려주는지 화면에서 직접 확인
   - 서버 쪽 로그에 남은 처리 순서(접수 → 수신 → 처리 시작 → 완료 → 삭제)와 화면에서 본 순서가 일치하는지 대조

참조: `apps/agent/src/front/main.ts`, `packages/agent_domain/src/front/model/event-stream.ts`

## 슬라이드 15 — 실시간 전달의 부하: 어디서 생기고 어떻게 푸는가

핵심: 이벤트를 만드는 부하와, 그걸 접속자 전원에게 계속 실어나르는 부하는 완전히 다른 종류다 — 하나를 해결해도 다른 하나는 저절로 안 풀린다

1. 부하가 생기는 자리는 두 곳이다
   - 만드는 쪽(생산): 이벤트 하나 만들고 큐에 넣고 처리하면 끝나는 순간적인 일감 — 처리기를 여러 대로 나눠두면 서로 몰라도 알아서 나눠 가짐
   - 실어나르는 쪽(전달): 접속해 있는 사람 한 명 한 명에게 연결을 계속 붙잡고 소식을 흘려보내야 함 — 끝나는 일이 아니라 접속해 있는 내내 이어지는 부담
   - 비유: 방송국에서 뉴스를 "만드는" 인력은 몇 명이면 충분하지만, 그 뉴스를 "듣는 사람 수만큼" 송출 채널을 계속 열어둬야 함 — 시청자가 늘면 커지는 건 제작 부담이 아니라 송출 부담
2. 왜 전달 부하는 한곳에 몰리기 쉬운가
   - 접속자 수와, 그 사람들이 듣는 채널 수를 곱한 만큼 "누구에게 뭘 보낼지" 계속 확인하고 실어날라야 함
   - 이 확인·전달을 서버 한 대만 맡고 있으면, 사람이 늘수록 부하가 전부 그 한 대에 쌓임 — 처리기(만드는 쪽)를 아무리 늘려도 이 부담은 안 줄어듦
   - "이벤트를 빨리 만드는 능력"과 "그 결과를 많은 사람에게 동시에 실어나르는 능력"은 서로 다른 능력이라 따로 키워야 함
3. 해소법 A — 담당을 나눠 갖는다(분산)
   - 접속자를 여러 대의 서버로 흩어 붙임 — 한 대가 감당할 인원을 정해두고 그 이상은 다른 서버가 맡음
   - 대가: A서버에 붙은 사람이 궁금해하는 소식이 B서버에서 만들어졌다면, A서버는 원래 그걸 모름 — 서버들끼리 "이런 소식이 생겼다"를 서로 알려주는 별도의 중계 통로가 반드시 있어야 함(방송사끼리 뉴스를 공유하는 중계망과 같음)
4. 해소법 B — 관심 없는 건 애초에 안 보낸다(구독 정밀화)
   - 지금처럼 "일단 다 돌려보고 관심 있는지 하나하나 확인"하는 대신, "이 사람이 무엇을 듣고 있는지"를 미리 정리해두고 그 사람에게 필요한 것만 골라 보냄
   - 접속자 전체 수와 무관하게 각자에게 필요한 만큼만 계산하면 되므로, 사람이 늘어도 개인별 비용만 늘어남
5. 해소법 C — 계속 붙어있지 않는다(연결 방식 자체를 바꿈)
   - 연결을 실시간으로 계속 열어두는 대신, 필요할 때만 잠깐 물어보고 끊는 방식으로 바꾸면 서버가 동시에 붙잡고 있어야 하는 연결 수 자체가 줄어듦
   - 대가: "지금 막 생긴 소식"을 즉시 받아보기 어려워짐 — 실시간성과 서버 부담은 항상 맞바꾸는 관계
6. 정리 — 부하를 없애는 방법은 없다, 옮기고 고르는 것뿐이다
   - 분산은 서버 간 중계 통로가 필요하고, 구독 정밀화는 구독 목록을 정확히 관리하는 비용이 들고, 연결 방식 변경은 실시간성을 일부 포기함
   - 세 가지는 서로 배타적이지 않음 — 규모가 커질수록 세 가지를 함께 적용하는 게 보통

참조: (개념 정리 슬라이드 — 특정 구현 코드 참조 없음. 앞선 스트림 허브 슬라이드에서 관찰한 실제 부하 지점을 일반화한 것)
노트: 이 슬라이드는 "지금 코드를 어떻게 고칠지"가 아니라 "왜 이런 종류의 문제가 생기고, 선택지가 무엇인지"만 다룬다. 구체적 해법 선택은 실제 접속자 규모가 나온 뒤에 결정할 문제.

---

## 부록 0. 런타임 구성 참고 (슬라이드에는 넣지 않고 구두로만)

- `admin` 컨테이너 = PostgreSQL 18 + pgvector + PGMQ + 관리자 Node 서버, `agent` 컨테이너 = Node 런타임만.
- 전용 네트워크 1개, 호스트 노출은 18080/18081, 상태는 `postgres_data`/`admin_data` 볼륨.
- 큐는 initdb에서 `agent_requests`/`agent_results` 2개 생성. 볼륨이 남아 있으면 initdb는 다시 돌지 않음.
- `agent_execution`/`agent_events`는 에이전트가 기동 시 `IF NOT EXISTS`로 보장(현재 initdb 정의와 드리프트 있음).
- 배포는 `npm run deploy` 단일 진입점, 이미지는 호스트에서 빌드한 `dist`만 담는 런타임 전용 구성.

## 부록 A. 라이브 데모 시나리오 (15분)

1. `npm run deploy` 후 `/ready` 200 확인.
2. 콘솔에서 `session.create.request` 전송 → 아웃바운드 1건, 인바운드 결과 1건.
3. 같은 transactionKey로 재전송 → 워커 로그 없이 결과만 재생되는 것 확인.
4. 같은 transactionKey에 payload를 바꿔 전송 → `idempotency_conflict` 확인.
5. `payload.simulateDelayMs`를 길게 준 요청 실행 중 가시성 연장 로그(`set_vt`) 확인.
6. `payload.timeoutMs` 초과 요청으로 `timed_out` 상태 기록 확인.

## 부록 B. 실습 과제

1. 새 이벤트 패밀리(`memory.*`) 파일을 추가하고 `VibeEvent` 유니온에 합류시키기.
2. 워커 허용 액션에 `text.tokenize`를 추가하고 결과 이벤트로 회수하기.
3. `sequence` 커서를 이용해 구독 시점 이전 이벤트를 재생하는 `stream.replay.request` 설계하기(설계만).

## 부록 C. 다음 강의 예고 (#5)

- 컨텍스트 저장소와 컴팩트 실행 본체(`state.*` 구현)
- 모델 어댑터와 모델라우터 결합(`model.*` 구현)
- 도구를 워커 스레드에서 외부 프로세스로 승격(`process.*` 구현)
