# Testing

Run `npm run lint --workspace agent` and `npm exec turbo run build --filter=./apps/agent`. Use the Compose health endpoints and a queue round trip for integration verification after Docker is available.
