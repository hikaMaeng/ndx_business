# Overview

`agent` accepts self-describing events, delivers them through an interchangeable queue transport, and executes allow-listed actions in Node.js worker threads. The current deployment uses a PGMQ adapter.
