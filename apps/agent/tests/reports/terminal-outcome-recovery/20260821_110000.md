# Multi-channel transaction join report

## Revision and environment

- Revision under test: working tree after `84bee66`.
- Target: deployed `agent` container, `running/healthy`; `/health` returned 200.
- PostgreSQL and PGMQ were the deployed local stack. The shared result queue was not drained.

## Executed scenario

1. Submitted `test.delay` with `simulateDelayMs=30000`, transaction key `fanout-live-20260821015303`, and reply channel `agent.results`.
2. Two seconds later submitted the same action and payload with the same transaction key and reply channel `orders`.
3. Waited for terminal completion and queried `event_store`, `agent_execution_recipient`, `agent_execution`, and exact PGMQ result messages.

## Evidence

| Source | Observed terminal output |
| --- | --- |
| `event_store` | two commands, then `test.delay.result` on `agent.results` with `ok=true`, then `test.delay.result` on `orders` with `ok=true` |
| `agent_execution_recipient` | one durable recipient for `agent.results`, one for `orders` |
| `agent_execution` | `completed`, `ok=true` |
| PGMQ `agent_results` | one `ok=true` result addressed to `agent.results`, one addressed to `orders` |

The exact two PGMQ message IDs and all rows for this transaction were removed after observation. No unrelated queue messages or rows were removed.
