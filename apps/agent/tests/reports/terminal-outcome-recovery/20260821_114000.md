# Worker-failure multi-channel fan-out report

## Environment

Deployed `agent` container was `running/healthy`; `/health` returned 200. The shared PGMQ result queue was queried only by the exact transaction key.

## Executed scenario

Submitted `test.delay` twice with one transaction key and reply channels `agent.results` and `orders`. Both payloads used `simulateDelayMs=50` and `timeoutMs=5` to force the worker terminal failure.

## Evidence

| Check | Observed |
| --- | --- |
| `event_store` | one `worker_failed` result on `agent.results` and one on `orders`, both `ok=false` |
| `agent_execution` | `timed_out`, result error `worker_failed`, attempts `1` |
| PGMQ result queue | two messages, addressed to the two channels |
| Processing jobs | both `completed` |

All rows and exact result-queue messages for `workerfail-live-20260821023128` were deleted after observation.
