# Browser paged replay and reconnect report

## Environment

- Deployed Agent image: `sha256:f16b7ad8b5c8bac7912614d580e9ffa9e27b565e4c29eba11249d6f5e3132277`.
- Container health: `healthy`; `/health`: HTTP 200.
- Default replay page limit: 256 terminal events.

## Executed scenario

1. Drove the rendered `/` UI to add a generated reply channel and establish a durable cursor.
2. Took its browser context offline, closed its socket, and submitted 257 `hash.sha256` requests to that channel.
3. Restored the connection and observed canonical terminal frames in the UI connection.
4. Closed the connection once more to verify automatic reconnect with capped retry, then reloaded the page after the custom channel was gone.

## Result

| Check | Observed |
| --- | --- |
| Submitted/received terminal events | 257/257, unique event IDs |
| Replay paging | `replayComplete`: `false`, then `true` |
| Subscription pages | 3 |
| Reload after channel change | connected with default-channel cursor only; no fingerprint-rejection loop |
| Page errors | 0 |

Four browser console network errors occurred only while the test deliberately disabled the browser network; they are expected and are not page errors. The browser-composer action also received its real `session.create.request.result` over WebSocket.

## Cleanup

The generated `browser-replay-1787253644877-*` event, outbox, execution, recipient, processing, and PGMQ rows, plus the exact browser-composer transaction, are removed by the scoped cleanup. The three exact disposable projection identities created by the UI proof are removed with the same transaction.
