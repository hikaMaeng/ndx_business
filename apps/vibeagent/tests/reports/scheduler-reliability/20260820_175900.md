# Scheduler reliability and load report

## Revision and environment

- Revision: `f66140d`.
- Agent host: 48 logical CPUs; deployed worker ceiling: 96.
- Test ran on dedicated `agent_load_requests` and `agent_load_results` queues, then restored `agent_requests` and `agent_results`.

## Real PostgreSQL checks

| Check | Result |
| --- | --- |
| 2.5-second worker across heartbeat | `agent_execution=completed|1|true|true`; container `running/healthy` |
| Stale/current attempt completion | `0|1|2` (stale rejected, current accepted, two claims recorded) |
| Retry exhaustion at two attempts | first `ready`; second `failed`; final `failed|2|1` (one DLQ row) |
| Schema/indexes | `event_processing_job_claim_idx`, `event_processing_job_running_lease_idx`, `event_delivery_pending_idx`; status check includes `failed` |

The explicit heartbeat run exercised the deployed `ProcessingStore.renew()` SQL after one timer interval; the process remained healthy, so the former unhandled heartbeat rejection/crash path did not occur.

## 2,048-command drain run

- Work: 2,048 `test.delay` commands, 96 workers, 512 independent streams, 5,000 ms each.
- Ideal worker-only lower bound: `ceil(2048 / 96) * 5 s = 110 s`.
- Acceptance ceiling: 200 s (110 s + 90 s persistence/queue allowance).
- Measured durable window: **138.613 s** (`min(job.created_at)` to `max(execution.completed_at)`).
- Result delivery: 2,048 queue messages with 2,048 distinct transaction keys.
- Final dedicated queues: request 0; result 0 after verified drain.
- Final load metrics: `workerCompleted=2048`, `workerFailed=0`, `processingFailures=0`, `processingReady=0`, `processingRunning=0`, `processingExpiredLeases=0`, `deliveryPending=0`.

## Final deployment

Normal queue configuration was restored and `npm run deploy agent` completed. The final container is `running/healthy`; `/health` is 200; all active `/metrics` gauges (`processingReady`, `processingRunning`, `processingDlq`, `processingExpiredLeases`, `deliveryPending`) are zero.
