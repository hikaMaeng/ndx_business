# Conflict reclaim guard report

Deployed the payload-hash reclaim guard and submitted a conflicting request against an expired seeded execution lease.

| Check | Observed |
| --- | --- |
| Existing attempt owner | `seed-attempt` before and after request |
| Attempt count | `1` before and after request |
| Lease | remained expired (`lease_until < now() = true`) |
| Request result | one request-scoped `idempotency_conflict` on `orders` |

The exact result-queue message and all rows for `guard-live-20260821022926` were deleted after verification.
