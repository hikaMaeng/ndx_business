# Terminal outcome and recovery report

## Revision

- `df78647 fix(agent): preserve terminal outcomes across retries`
- Deployed through `npm run deploy agent`; `agent` became `running/healthy` and `/health` returned 200.

## Live PostgreSQL evidence

| Scenario | Result |
| --- | --- |
| canonical HTTP command | Stored command and result share `session:canonical-session`; result has the command event ID as `causation_event_id`, source `server`; execution is `completed|true`. |
| retry exhaustion | A matching active execution was paired with a job already at five attempts. Scheduler recorded `failed|6`, one DLQ row with the duplicate-running error, and a canonical result with `payload.error.code=processing_permanent_failure` and the request as causation. |
| expired execution reclaim | A matching execution with an expired lease was claimed by a new attempt and ended `completed|2|true`; the processing job ended `completed`. |

The injected verification rows and their exact result-queue messages were deleted after each scenario. Existing queues and unrelated rows were not drained.

## Static checks

- Agent lint passed.
- Agent tests: 17/17.
- agent tests: 3/3.
- Architecture and docs checks: `violations=0`.
