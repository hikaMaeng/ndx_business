# Fixed worker-pool 2,048 request report

## Environment

- Deployed Agent image: `sha256:316329a70aec7254d494ccf5fdf4567f6968c1506c5e86c8c91cd20113ce04e8`.
- Host CPU count observed by Agent: 48.
- Fixed resident worker capacity: 96 (`AGENT_MIN_THREADS=AGENT_MAX_THREADS=2 × CPU`).

## Workload

- 2,048 independent `test.delay` requests.
- 5,000 ms handler delay per request.
- 96 worker threads and 512 independent streams.

## Result

| Check | Observed |
| --- | --- |
| Worker pool before/after | 96 resident workers, 0 busy after drain, 0 queued |
| Theoretical worker lower bound | `ceil(2048 / 96) × 5,000 = 110,000 ms` |
| Event-store command-to-result window | 118,522 ms |
| Terminal results | 2,048/2,048 |
| Result queue messages | 2,048 distinct transaction keys |
| Execution/processing state | 2,048 completed, ready/running 0 |
| Failures/outbox backlog | 0 / 0 |
| Pool waiting | durable, ingress, queue pools all 0 |

The observed 8,522 ms overhead above the lower bound is within the 15,000 ms acceptance allowance and includes ingress, durable append, result outbox, and queue publication.

## Cleanup

The exact `delay-1787254381382-*` event-store, outbox, processing, execution, recipient, projection, and PGMQ result rows were deleted after observation. Every scoped count was verified at zero.
