{
  "name": "vibeagent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:front && npm run build:server",
    "build:front": "vite build",
    "build:server": "esbuild src/server/index.ts --bundle --platform=node --format=esm --target=node26 --alias:agent=../../packages/agent/dist --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" --outfile=dist/server/index.js",
    "lint": "tsc --noEmit -p tsconfig.server.json && tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "agent": "*",
    "vibeagent_domain": "*",
    "express": "5.2.1",
    "pg": "8.23.0"
  },
  "devDependencies": {
    "@types/express": "5.0.6",
    "@types/node": "26.1.1",
    "@types/pg": "8.21.0",
    "esbuild": "0.28.0",
    "tsx": "4.21.0",
    "typescript": "5.9.3",
    "vite": "8.0.11"
  }
}
